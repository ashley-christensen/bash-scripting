#Running multiple commands, repeatable ways of running commands

#One-liners -> presented in one line of text, command pipelines or series separated by ;
#can be very long and even wrap around in terminal --> one line has no line breaks
#often stored in a note for frequent use

# Bash cript = text file with a series of commands, run like a program
bash myscript.sh

