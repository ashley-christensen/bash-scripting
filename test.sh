#create shebang line = executable script
#!/usr/bin/env bash -> which program to use to run the script, here it's bash (interpretive scripting lang)


#OR
echo "The kernel is $(uname -r)"
#(bash test.sh) in the terminal
